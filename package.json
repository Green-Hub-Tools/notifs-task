{
  "name": "pr-task-notification",
  "version": "1.20.0",
  "description": "Pull Request notification for tasks",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.1",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
