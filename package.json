{
  "name": "pr-task-notification",
  "version": "1.20.0",
  "description": "Pull Request notification for tasks",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.js"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0",
    "axios": "^1.13.4"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
